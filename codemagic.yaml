workflows:
  ios-workflow:
    name: iOS Pure Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      java: 17
      xcode: latest
    scripts:
      - name: Clean Project
        script: |
          chmod +x gradlew
          ./gradlew clean
          
      - name: Build Kotlin Framework
        script: |
          set -e
          # Эти переменные критически важны для задачи syncComposeResourcesForIos
          export CONFIGURATION=Release
          export SDK_NAME=iphoneos
          export ARCHS=arm64
          export PLATFORM_NAME=iphoneos
          export IPHONEOS_DEPLOYMENT_TARGET=16.0
          
          echo "Сборка фреймворка с параметрами Xcode..."
          ./gradlew :sharedUI:embedAndSignAppleFrameworkForXcode \
            -Pkotlin.native.cocoapods.configuration=$CONFIGURATION \
            -Pkotlin.native.cocoapods.platform=$PLATFORM_NAME \
            -Pkotlin.native.cocoapods.archs=$ARCHS
          
      - name: Build Xcode Project
        script: |
          set -e
          PROJ_PATH=$(find . -name "iosApp.xcodeproj" -type d | head -n 1)
          PROJ_DIR=$(dirname "$PROJ_PATH")
          cd "$PROJ_DIR"

          xcodebuild build \
            -project "iosApp.xcodeproj" \
            -scheme "iosApp" \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            -derivedDataPath ./build-data

          mkdir -p Payload
          APP_PATH=$(find ./build-data -name "*.app" -type d | head -n 1)
          cp -R "$APP_PATH" Payload/
          zip -qr ../olcbox_ios.ipa Payload
    artifacts:
      - "*.ipa"
